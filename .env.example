# Hugging Face token (required for gated models)
HF_TOKEN=

# Cache directory for transcoders / model weights
HF_HOME=
TRANSFORMERS_CACHE=

# Device override (e.g. cuda, cpu, mps)
LLM_CIRCUITS_DEVICE=
